version: "3.8"

services:
  web:
    build: .
    command: python /code/manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - 8000:8000
    depends_on:
      - db
    # Note that if your SECRET_KEY includes a dollar sign, $, then you need to add an additional dollar sign, $$.
    environment:
      - "DJANGO_SECRET_KEY=django-insecure-$$#^n#eeraq_q9ecmxk5$tiqb1e&batwx_39*3*)-tz4v9qeo0+"
      - "DJANGO_DEBUG=True"
  
  db:
    image: postgres:16.3
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - "POSTGRES_HOST_AUTH_METHOD=trust"

volumes:
  postgres_data: